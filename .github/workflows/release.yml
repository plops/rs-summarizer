name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    name: Build and Release
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: rs-summarizer
            asset_name: rs-summarizer-linux-amd64
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: rs-summarizer
            asset_name: rs-summarizer-macos-amd64
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: rs-summarizer
            asset_name: rs-summarizer-macos-arm64

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2

      - name: Build release
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package asset
        shell: bash
        run: |
          # Create a staging directory with the binary and required runtime files
          mkdir -p staging
          cp target/${{ matrix.target }}/release/${{ matrix.artifact_name }} staging/
          cp -r static staging/
          tar czf "${{ matrix.asset_name }}.tar.gz" -C staging .

      - name: Upload assets to release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            ${{ matrix.asset_name }}.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
